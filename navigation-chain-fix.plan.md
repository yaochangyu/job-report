# 頁面導航鏈路修正計畫

## 目標

把目前僅統計 `previousPageUrl -> pageUrl` 單跳轉換的實作，改為真正依 `session_id + occurred_at` 重建的**完整 session 頁面導航鏈路**，並同步修正：

- 單頁前端（DuckDB-WASM）
- 舊版 Python 報表

## 實作步驟

- [x] **步驟 1：盤點現有資料欄位與鏈路定義**
  - 需要先確認 `session_id`、`occurred_at`、`page_path`、`previous_page_path` 在 dataset 與舊報表查詢中的可用性。
  - 這一步也會明確定義「完整鏈路」的切法，例如是否以單一 session 全程路徑作為一條鏈路、是否保留重複頁面跳轉。

- [x] **步驟 2：修正單頁前端查詢邏輯為完整鏈路排行**
  - 需要把 `frontend/query-definitions.js` 的 navigation 查詢，從雙欄位聚合改為依 session 與時間排序後組裝鏈路。
  - 這樣前端顯示的資料才會與「頁面導航鏈路」名稱一致，不再只是單跳流向排行。

- [x] **步驟 3：修正單頁前端 renderer 與 KPI 呈現**
  - 查詢結果形狀改變後，`frontend/dashboard-renderers.js` 與相關文案也要同步調整，否則圖表與 KPI 會錯讀資料。
  - 這一步會確保頁面上顯示的是鏈路數、Top 鏈路排行等與新資料模型一致的內容。

- [x] **步驟 4：修正舊版 Python 報表查詢與輸出**
  - `page_navigation_report.py` 目前同樣是做單跳聚合，需改成輸出完整 session 鏈路排行。
  - 這樣新舊兩條產出路線的定義才會一致，避免同名報表卻呈現不同邏輯。

- [x] **步驟 5：同步更新文件與專案結構描述**
  - `README.md` 若仍描述為來源頁/目標頁統計，會與實際行為不符，需要一併修正。
  - `tree.md` 也必須維持最新狀態，確保專案結構文件一致。

- [x] **步驟 6：build 並確認產出可正常使用**
  - 功能完成後需要重新組裝單頁輸出與確認舊版報表可產生結果。
  - 這一步是為了避免查詢改完後，前端或報表輸出格式不相容。

## 備註

- 本次調整依你的決定，**直接改成只顯示完整 session 鏈路排行**，不再保留現有單跳排行作為主要輸出。
- 若後續需要補回單跳流向視角，可另外作為次要分析卡片，而不是繼續占用「頁面導航鏈路」主畫面。
- 鏈路定義確定為：**同一查詢區間內，以 `session_id + occurred_at` 排序後的 `view` 事件 page 序列作為一條鏈路，並移除連續重複頁面；僅保留至少 2 步的 session。**
