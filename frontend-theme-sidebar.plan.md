# Frontend Theme Sidebar 實作計畫

## 目標
在 `dev-1` 分支上調整單頁前端風格，降低與既有專案視覺落差，並完成以下兩項需求：

- 新增暗黑 / 光亮版切換
- 分析角度改為左側側邊導覽，不再使用下拉選單

## 設計方向

- 光亮版配色以 `https://yaochangyu.github.io/job-report/` 為主，沿用淺灰背景、深藍 header、白色卡片、低陰影的視覺語言。
- 暗黑版保留目前較深色的風格，但要改成和光亮版共用同一套 layout 與 spacing。
- 版型改為左側固定或 sticky 側邊導覽，右側為查詢條件、KPI、圖表與表格內容。
- 查詢條件保留日期與頁面路徑；分析角度改由左側導覽切換，不再放在表單下拉。

## 實作步驟

- [x] Step 1：建立主題系統與亮暗切換
  - 需要先把目前寫死的深色樣式抽成 theme token，否則後面很難同時維護亮色與暗色。
  - 光亮版的背景、header、card、邊框、文字與 accent 色都要明確對齊既有站台配色，這一步是整體視覺一致性的基礎。

- [x] Step 2：重構頁面版型為左側導覽 + 右側內容
  - 需要先把目前單欄式結構拆成側邊導覽與主內容區，才能把分析角度從下拉選單移到左側。
  - 這一步也會一起調整區塊層級，避免查詢條件、狀態區、KPI 與圖表仍維持舊版配置。

- [x] Step 3：重接分析角度切換與查詢互動
  - 分析角度一旦改成左側導覽，就要把目前 `view_mode` 的來源從表單改成導覽選項，並確保 DuckDB 查詢仍使用正確的模式。
  - 同時要確保主題切換、日期篩選、頁面路徑與查詢按鈕在新 layout 下仍正常運作。

- [x] Step 4：統一元件風格並驗證實際查詢畫面
  - 需要把 hero、panel、KPI 卡、圖表容器、表格與查詢記錄區塊一起收斂到新風格，避免只有 header 和導覽更新，內容區仍像另一套設計。
  - 最後要以實際查詢後的畫面確認亮暗版、側邊導覽與內容區在長資料情況下仍穩定。

## 注意事項

- 只在 `dev-1` 分支調整，不影響 `main`
- 亮色版以現有 GitHub Pages 導覽頁為主要參考，不額外自行發明全新色系
- 側邊導覽需保留目前 8 種分析視角
- 需要避免再次出現查詢後圖表高度失控或區塊被內容撐爆的問題

## 本次產出

- 新增亮色 / 暗色 theme token 與切換按鈕
- 亮色版配色對齊既有 GitHub Pages 導覽頁的淺灰背景、深藍 header、白色卡片風格
- 移除分析角度下拉選單，改為左側側邊導覽
- 側邊導覽與查詢互動重新接線，查詢後仍沿用既有 DuckDB SQL 流程
