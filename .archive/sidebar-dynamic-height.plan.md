# sidebar-dynamic-height 實作計畫

- [x] 重新確認側邊導覽最後一筆仍無法完整捲入可視區的根因，因為目前固定 `100vh` 高度沒有扣掉導覽框當前距離視窗頂部的位置，需先明確改成動態高度模型。
- [x] 在前端加入側邊欄高度同步邏輯，依 `.sidebar` 當前 `top` 與 viewport 高度計算可用空間，讓導覽列在進入 sticky 前後都能完整捲到底。
- [x] 調整 CSS 改用 JS 寫入的高度變數，保留桌機版 sticky 與手機版原本行為，避免再次出現可捲區落在視窗外。
- [x] 重新 build HTML 輸出並同步更新 `tree.md`；任務完成後封存計畫檔，確保專案結構紀錄一致。

## 失敗紀錄

- 首次套用 patch 時用錯 `main()` 上下文，實際檔案入口是 `bootstrap()`，導致 `app.js` hunk 無法套用。後續重試直接比對實際函式名稱與位置，不再使用已失敗的舊上下文。
