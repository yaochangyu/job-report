# sidebar-scroll-container 實作計畫

- [x] 重新確認目前側邊導覽的滾動模型與上一次修正結果，因為前一版只讓內層選單可捲，需先明確記錄這次要改成整個導覽框可捲，避免沿用同樣問題模型。
- [x] 改寫側邊欄 CSS，將 `.sidebar` 調整為固定高度的可捲容器，並讓 header 固定在框內，降低觸控板事件被右側主卷軸搶走的機率。
- [x] 重新 build HTML 輸出並確認 `output/` 已同步新的側邊捲動行為，避免部署時仍帶舊版樣式。
- [x] 更新 `tree.md` 與計畫狀態；若任務完成則封存計畫檔，確保專案結構紀錄一致。
