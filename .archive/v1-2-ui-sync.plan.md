# v1-2 UI Sync Plan

- [x] 盤點 `v1-1` 與目前 `v1-2` 的 UI 差異，確認要搬移的前端資產、入口頁與資料查詢腳本。這一步是必要的，因為 `v1-1` 看起來是完整 SPA，不只是 CSS；若只套樣式，畫面與互動不會真正一致。
- [x] 將 `v1-1` 的 UI 程式碼合到 `v1-2`，並保留 `v1-2` 現有的資料來源與版本部署方式。這一步是必要的，因為目標是讓 `https://yaochangyu.github.io/job-report/v1-2/` 的 UI 跟 `v1-1` 一樣，但不能把 `v1-2` 既有資料流程一起破壞掉。
- [x] 調整產出與部署流程，確保部署到 GitHub Pages 的 `v1-2` 版本會包含新的 UI 檔案。這一步是必要的，因為就算本機整合成功，若部署流程沒帶到對應檔案，線上結果還是會不一致。
- [x] build 驗證產出結果，並在你確認需要時再執行測試。這一步是必要的，因為要確保修改後的頁面真的能產生並部署，不只是檔案有改到。
