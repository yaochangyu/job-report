# Layout Adjustment 實作計畫

## 目標
調整單頁報表版型，讓共用查詢條件移到上方，同時讓左側導覽可以展開 / 收合，騰出更多報表呈現空間。

## 實作步驟

- [x] Step 1：把共用查詢條件移到主內容上方
  - 查詢條件屬於所有分析視角共用的控制列，應該放在主要內容區最上方，避免看起來像某一個視角專屬設定。
  - 這一步需要重排 `index.html` 區塊順序與對應 CSS，確保查詢列在視覺上先於目前視角資訊、KPI 與圖表。

- [x] Step 2：為左側導覽加入伸縮控制
  - 需要增加可點擊的伸縮按鈕，讓使用者在需要閱讀更多圖表或表格資訊時，可以收合側邊導覽。
  - 這一步會調整 `app.js` 的版型狀態與 `app.css` 的 grid/寬度規則，讓收合後主內容區實際變寬，而不是只把導覽內容藏起來。

- [x] Step 3：驗證版面與互動一致性
  - 查詢列改到上方、導覽可伸縮後，要確認導覽切換、自動查詢、主題切換與查詢結果渲染沒有被影響。
  - 也要實際檢查收合前後的內容區寬度與資訊密度，避免版型跑掉或可用空間沒有真的增加。

## 注意事項

- 維持目前「導覽切換自動查詢 / 沿用上次成功查詢條件」的行為
- 不變更 DuckDB 查詢定義，只調整畫面結構與前端互動
- 伸縮後仍要保留分析視角的可辨識性，避免只剩 icon 導致難以理解

## 本次產出

- 共用查詢條件已移到主內容最上方，切換視角時仍沿用同一組查詢條件
- 左側導覽新增收合 / 展開按鈕，收合後主內容區寬度會實際增加
- 收合狀態會記住，上次的版型偏好在重新載入後仍會保留
- 已驗證查詢列位置、側邊收合後內容區變寬，以及導覽切換查詢仍正常
