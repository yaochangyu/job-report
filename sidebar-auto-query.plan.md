# Sidebar Auto Query 實作計畫

## 目標
調整左側導覽切換行為，讓單頁報表在切換分析視角時不需要再手動按一次「執行查詢」：

- 若尚未成功執行過查詢，切換導覽時直接以目前表單條件自動查詢
- 若已成功執行過查詢，切換導覽時沿用上次成功查詢的日期與頁面條件，自動更新目前視角

## 實作步驟

- [x] Step 1：整理導覽切換與查詢狀態
  - 需要先定義前端要保存哪些狀態，例如「是否曾成功查詢」與「上次成功查詢條件」，否則導覽切換時不知道要沿用哪一組條件。
  - 這一步也會確認首次切換與後續切換的行為分界，避免切換視角後套用到尚未確認的輸入值。

- [x] Step 2：重構側邊導覽的查詢觸發流程
  - 需要把目前單純更新 `selectedViewMode` 的行為，改成可共用既有查詢邏輯的流程，避免切換導覽與按下查詢按鈕走兩套不同邏輯。
  - 已查詢過時要沿用上次成功查詢條件；尚未查詢過時要使用目前表單值直接查詢。

- [x] Step 3：驗證切換導覽與查詢結果一致
  - 需要確認首次切換、已查詢後切換、修改表單再手動查詢等情境都能得到一致結果。
  - 也要避免因為自動查詢而讓 KPI、圖表、表格或查詢摘要顯示不一致。

## 注意事項

- 只調整 `dev-1` 分支上的單頁前端互動
- 不修改 DuckDB SQL 定義，只調整前端狀態管理與事件流程
- 若查詢失敗，不應把失敗條件覆寫成「上次成功查詢條件」

## 本次產出

- 新增 `lastSuccessfulFilters` 狀態，保存最近一次成功查詢的日期與頁面條件
- 手動查詢與側邊導覽切換共用同一條 `runQuery()` 流程，避免行為分叉
- 側邊導覽切換時：
  - 尚未成功查詢過：直接用目前表單條件自動查詢
  - 已成功查詢過：回填並沿用上次成功查詢條件，自動更新新視角
